{
  "dependencies": {
    "@tensorflow/tfjs": "^3.20.0",
    "@tensorflow/tfjs-node-gpu": "^3.20.0",
    "socket.io": "^4.5.1",
    "text-to-speech-js": "^1.1.10"
  },
  "scripts": {
    "start": "http-server -S -C ./certs/cert.pem -K ./certs/key.pem -o"
  }
}
